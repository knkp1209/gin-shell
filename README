通过 web 执行脚本

构建并开启服务
mv ./shell/env_example.sh
# vim ./shell/env_example.sh
# 配置好脚本环境变量
go build && ./gin-shell

客户端请求
示例1：
curl 'http://localhost:7790/fetch'
示例2：
curl 'http://localhost:7790/checkout?branch=feature/yangwenhang/buried-point'